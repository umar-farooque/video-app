import VideosReducer from '../features/videos';

// We're Exporting Single Reducer because we only have one reducer,if we have multiple reducers then we can combine the reducers and export it.
export default VideosReducer;
